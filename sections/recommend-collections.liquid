{% comment %}
  Section: Recommend Collections
  功能：
  - 在系列页展示 recommend 元字段（collection list）
  - 展示名称优先使用系列的 bieming 元字段，否则回退到系列标题
  - PC 端居中，移动端横向滚动
{% endcomment %}

{% if collection and collection.metafields.custom.recommend != blank %}
  <div class="recommend-collections-wrapper page-width">


    <ul class="collection-list">
      {% assign recommend_list = collection.metafields.custom.recommend.value %}
      {% for rec in recommend_list %}
        {% assign display_name = rec.metafields.custom.bieming | default: rec.title %}
        <li class="collection-item" data-title="{{ display_name | downcase }}">
          <a href="{{ rec.url }}" class="collection-link">
            {{ display_name }}
          </a>
        </li>
      {% endfor %}
    </ul>
  </div>
{% endif %}

<style>
  .recommend-collections-wrapper {
    margin: 20px 0;
    max-width: 1200px; /* 限制最大宽度，保证居中效果 */
    margin-left: auto;
    margin-right: auto;
  }

  .recommend-title {
    font-size: 18px;
    font-weight: 600;
    margin-bottom: 12px;
    text-align: center; /* 标题居中 */
  }

  .collection-list {
    list-style-type: none;
    padding: 0;
    margin: 0;
    display: flex;
    gap: 12px;
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
    justify-content: center; /* PC 端居中 */
  }

  .collection-list::-webkit-scrollbar {
    display: none;
  }

  .collection-item {
    flex: 0 0 auto;
    border: 1px solid #000;
    padding: 8px 16px;
    text-align: center;
    background-color: #fff;
    border-radius: 6px;
    white-space: nowrap;
  }

  .collection-link {
    text-decoration: none;
    color: #000;
    font-size: 14px;
    font-weight: 500;
  }

  .collection-link:hover {
    text-decoration: underline;
  }

  /* 移动端保持横向滑动 */
  @media (max-width: 768px) {
    .collection-list {
      justify-content: flex-start;
    }
  }
</style>

{% schema %}
{
  "name": "Recommend Collections",
  "tag": "section",
  "class": "recommend-collections",
  "settings": [
    {
      "type": "checkbox",
      "id": "show_title",
      "label": "显示标题",
      "default": true
    },
    {
      "type": "text",
      "id": "title",
      "label": "标题文本",
      "default": "Collections recommandées"
    }
  ],
  "presets": [
    {
      "name": "Recommend Collections"
    }
  ]
}
{% endschema %}
